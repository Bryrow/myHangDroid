<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="edu.slcc.briannielson.splash.ScoreActivity"
    android:background="@color/splash_darkgray">

    <ScrollView
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:id="@+id/scrollView">
    
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/textViewScores"
            android:textColor="@color/cyan"
            android:textSize="30dp"
            android:text="Brian is Great!"/>

    </ScrollView>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAllCaps="false"
        android:id="@+id/clearScores"
        android:text="Clear Scores"
        android:onClick="clearScores"
        android:layout_alignTop="@+id/scrollView"
        android:layout_alignParentEnd="true"
        android:background="@drawable/button_style" />


</RelativeLayout>
